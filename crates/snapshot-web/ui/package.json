{
  "name": "snapshot-web-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "wasm": "cd .. && wasm-pack build . --target web --out-dir ui/src/generated/snapshot_web --release",
    "dev": "npm run wasm && vite",
    "build": "npm run wasm && npm run typecheck && vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.worker.json && tsc --noEmit -p tsconfig.test.json && tsc --noEmit -p tsconfig.node.json",
    "test": "vitest run",
    "test:browser": "wasm-pack test --headless --chrome .."
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4",
    "wasm-pack": "^0.13.1"
  }
}
